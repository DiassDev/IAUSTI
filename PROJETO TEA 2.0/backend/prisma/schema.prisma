// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}


// Tabelas do Banco de dados
model User {
  id        String   @id @default(uuid())
  name      String
  email     String   @unique
  password  String?  // Null para usuários que usam Google OAuth
  googleId  String?  @unique // ID do Google OAuth
  picture   String?  // URL da foto de perfil
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt


  @@map("users")
}

// Construir a tabela dos Cursos
model Cursos { 

}

// Construir a tabela de Aulas
model Aulas {

}